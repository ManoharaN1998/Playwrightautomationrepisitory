//8. Write a program to display Mathematical Table for a given number
let num=5
for(let i=1;i<=10;i++)
    {
        console.log(num + "*" + i + "=" + (num * i));
    }
    console.log("....................")
// ------------------------------------------------------------------
let num1=5
let j=1
while(j<=10)
    {
        console.log(num1 + "*" + j + "=" + (num1 * j))
         j++
    }
    console.log("....................")
// ------------------------------------------------------------------
let k=1
let num2=5
do{
    console.log(num2 + "*" + k + "=" + (num2 * k))
    k++
}while(k<=10)
// ------------------------------------------------------------------
